version: '2'
sql:
  - schema: 'sql/schema'
    queries: 'sql/queries'
    engine: 'postgresql'
    gen:
      go:
        out: internal/database
        overrides:
          - db_type: 'email_address'
            go_type: 'string'
          - db_type: 'username'
            go_type: 'string'
